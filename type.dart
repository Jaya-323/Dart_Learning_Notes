void main() {
  //explicitly giving types to the variable rather than allowing dart to know itself the type
  String name = "abc";
  print(name);
  int?
  n; //if we have some variable in which we want to be null we should ? with variable name
  print(n);
}
